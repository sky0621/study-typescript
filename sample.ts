class Point {
    x: number
    y: number
    constructor(x: number, y: number) {
        this.x = x
        this.y = y
    }
}

var p = new Point(1, 2)
console.log(p)
console.log(p.x)
console.log(p.y)
